@extends('layouts.admin') @section('title','Support Settings') @section('content-header')<h1>Support Settings <small>Notifications, SLA and automation</small></h1>@endsection @section('content')<div class="row"><div class="col-md-8"><form method="POST" action="{{route('admin.tickets.settings.save')}}">@csrf<div class="box box-primary"><div class="box-header with-border"><h3 class="box-title">Ticket automation</h3></div><div class="box-body"><div class="form-group"><label>Discord webhook</label><input class="form-control" type="url" name="discord_webhook" value="{{$settings['discord_webhook']}}" placeholder="https://discord.com/api/webhooks/..."><p class="help-block">New tickets and customer replies are sent to this webhook. Leave empty to disable Discord notifications.</p></div><div class="checkbox"><label><input type="checkbox" name="auto_assign" value="1" @checked($settings['auto_assign']==='1')> Enable automatic staff assignment</label></div></div></div><div class="box box-info"><div class="box-header with-border"><h3 class="box-title">SLA response targets</h3></div><div class="box-body"><div class="row">@foreach(['urgent'=>'Urgent','high'=>'High','normal'=>'Normal','low'=>'Low'] as $key=>$label)<div class="col-sm-6"><div class="form-group"><label>{{$label}} (minutes)</label><input class="form-control" type="number" min="15" max="10080" name="sla_{{$key}}" value="{{$settings['sla_'.$key]}}"></div></div>@endforeach</div><p class="help-block">The target is recalculated when a customer replies or ticket priority changes.</p></div><div class="box-footer"><button class="btn btn-primary">Save support settings</button></div></div></form></div><div class="col-md-4"><div class="box"><div class="box-header"><h3 class="box-title">Email notifications</h3></div><div class="box-body"><p>Ticket email uses Nodexa's existing Laravel mail configuration.</p><p class="text-muted">Customers receive mail when a ticket is created, answered or closed.</p><a class="btn btn-default btn-block" href="{{route('admin.settings.mail')}}">Mail settings</a></div></div><div class="box"><div class="box-header"><h3 class="box-title">Discord</h3></div><div class="box-body"><p>The webhook is optional. No Discord token is required.</p><p class="text-muted">Webhook failures are logged and do not block ticket actions.</p></div></div></div></div>@endsection