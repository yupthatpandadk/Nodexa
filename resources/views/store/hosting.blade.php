@extends('store.layout') @section('title','Server Hosting') @section('content')<section class="pagehero"><div class="container"><div class="eyebrow">Hosting</div><h1>Game server hosting.</h1><p class="lead">Vælg en aktiv Nodexa-pakke og konfigurér din server.</p></div></section><section class="section"><div class="container"><div class="grid">@forelse($products as $p)<article class="card" style="{{$p->featured?'border-color:#7557ff;box-shadow:0 15px 45px #4c36c52b':''}}"><div class="eyebrow">{{$p->featured?'★ Populær · ':''}}{{$p->game}}</div><h2 style="font-size:25px;margin-bottom:5px">{{$p->name}}</h2><p class="muted">{{$p->description ?: 'Klar til Nodexa Control Panel.'}}</p><div class="price">{{number_format($p->price_monthly,0,',','.')}} kr <small>/ måned</small></div><div class="specs"><span>{{$p->memory}} MB RAM</span><span>{{$p->cpu}}% CPU</span><span>{{$p->disk}} MB disk</span><span>{{$p->backups}} backups</span></div><a style="width:100%;margin-top:20px" class="btn primary" href="{{route('store.product',$p)}}">Konfigurér & bestil →</a></article>@empty<div class="card" style="grid-column:1/-1;text-align:center"><h2>Serverpakker kommer snart</h2><p class="muted">Der er endnu ingen aktive produkter.</p></div>@endforelse</div></div></section>@endsection