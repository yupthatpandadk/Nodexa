@extends('store.layout') @section('title','Mine ordrer') @section('content')
<section class="pagehero" style="padding-bottom:42px"><div class="container"><div class="eyebrow">Kundeområde</div><h1>Mine ordrer.</h1><p class="lead">Se status på dine serverbestillinger og fortsæt igangværende checkout.</p></div></section>
<section class="section" style="padding-top:35px"><div class="container" style="max-width:1050px">@forelse($orders as $o)<article class="card" style="margin-bottom:13px;display:flex;justify-content:space-between;align-items:center;gap:25px"><div><div class="eyebrow">Ordre #{{$o->id}}</div><h3 style="margin:5px 0">{{$o->product->name??'Serverprodukt'}}</h3><div class="muted">{{$o->server_name}} · {{number_format($o->amount,2,',','.')}} {{$o->currency}}</div></div><div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap"><span class="tag">{{str_replace('_',' ',$o->status)}}</span>@if($o->server)<a class="btn primary" href="/server/{{$o->server->uuidShort}}">Åbn server →</a>@elseif($o->status==='awaiting_payment')<a class="btn primary" href="{{route('store.checkout',$o)}}">Fortsæt checkout →</a>@endif</div></article>@empty<div class="card" style="text-align:center;padding:48px"><div class="icon" style="margin:auto">▤</div><h2>Ingen ordrer endnu</h2><p class="muted">Når du bestiller en server, vises den her.</p><a class="btn primary" href="{{route('store.hosting')}}">Se server hosting →</a></div>@endforelse</div></section>@endsection