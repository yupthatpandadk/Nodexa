@extends('store.layout') @section('title','Mine ordrer') @section('content')
<section class="pagehero" style="padding-bottom:45px"><div class="container"><div class="eyebrow">Billing & Orders</div><h1>Mine ordrer.</h1><p class="lead">Et samlet overblik over dine serverbestillinger og deres status.</p></div></section>
<section class="section" style="padding-top:38px"><div class="container" style="max-width:1050px"><div class="sectionhead"><div><div class="sectionNo">CUSTOMER AREA</div><h2>Ordrehistorik</h2></div><a class="btn primary" href="{{route('store.hosting')}}">＋ Ny server</a></div>@forelse($orders as $o)<article class="card" style="margin-bottom:12px;display:flex;justify-content:space-between;align-items:center;gap:24px"><div><div class="miniLabel">ORDER #{{$o->id}}</div><h3 style="margin:5px 0">{{$o->product->name??'Serverprodukt'}}</h3><div class="muted" style="font-size:13px">{{$o->server_name}} · {{number_format($o->amount,2,',','.')}} {{$o->currency}}</div></div><div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap"><span class="statusBadge">{{str_replace('_',' ',$o->status)}}</span>@if($o->server)<a class="btn primary" href="/server/{{$o->server->uuidShort}}">Åbn server →</a>@elseif($o->status==='awaiting_payment')<a class="btn primary" href="{{route('store.checkout',$o)}}">Checkout →</a>@endif</div></article>@empty<div class="card emptyState"><div class="icon">▤</div><h2>Ingen ordrer endnu</h2><p class="muted">Din første serverbestilling vil blive vist her.</p><a class="btn primary" href="{{route('store.hosting')}}">Find en server →</a></div>@endforelse</div></section>@endsection