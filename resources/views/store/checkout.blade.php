@extends('store.layout') @section('title','Checkout') @section('content')
<section class="pagehero" style="padding-bottom:42px"><div class="container"><div class="eyebrow">Sikker checkout</div><h1>Gennemgå din ordre.</h1><p class="lead">Kontrollér serveren og prisen før betaling.</p></div></section>
<section class="section" style="padding-top:35px"><div class="container" style="max-width:1000px"><div class="checkout-grid"><div class="card"><div class="eyebrow">Ordre #{{$order->id}}</div><h2 style="margin:8px 0 3px">{{$order->product->name}}</h2><p class="muted">{{$order->server_name}}</p><div style="margin-top:22px">@foreach(['Servernavn'=>$order->server_name,'RAM'=>$order->product->memory.' MB','CPU'=>$order->product->cpu.'%','Disk'=>$order->product->disk.' MB','Backups'=>$order->product->backups,'Databaser'=>$order->product->databases] as $label=>$value)<div style="display:flex;justify-content:space-between;gap:20px;padding:13px 0;border-bottom:1px solid var(--line)"><span class="muted">{{$label}}</span><strong>{{$value}}</strong></div>@endforeach</div></div><aside class="card featured"><span class="tag">Ordreoversigt</span><div class="price">{{number_format($order->amount,2,',','.')}} kr <small>/ måned</small></div><p class="muted" style="font-size:13px">Din ordre står som afventende betaling. Automatisk betaling kobles på i næste billing-trin.</p><button class="btn primary" style="width:100%;opacity:.55" disabled>Betaling kommer snart</button><a class="btn" style="width:100%;margin-top:9px" href="{{route('store.orders')}}">Mine ordrer</a></aside></div></div></section>
@push('styles')<style>.checkout-grid{display:grid;grid-template-columns:minmax(0,1.4fr) minmax(300px,.6fr);gap:20px}@media(max-width:760px){.checkout-grid{grid-template-columns:1fr}}</style>@endpush @endsection