# Pterodactyl's frontend currently contains legacy React 16 dependencies whose
# peer dependency ranges are rejected by modern npm (npm 7+), even though the
# dependency set is the one used by the panel. Keep this scoped to the Nodexa
# panel rather than changing npm globally on the host.
legacy-peer-deps=true
fund=false
audit=false
