# Branch

Development branch: `feature/moderation-runtime`.

Keep production deployments on stable `main` until this branch passes its Discord test-guild validation. The branch can be reviewed without changing the currently installed Nodexa Update Center version.
