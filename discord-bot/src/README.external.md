# External dependencies

Production validation depends on the user's Discord application and guild: bot authorization, guild ID, privileged intents, actual roles/channels and permission overwrites. These values are deliberately not embedded in repository source.
