# Log routing

Case actions prefer the configured case log channel and fall back to the primary moderation log. AutoMod/raid events prefer the AutoMod log and fall back to the primary log. Missing/invalid log channels should not prevent the underlying moderation action from completing.
