# Stable release process

After test-guild validation, merge the runtime together with the final admin/runtime integration fixes and update Nodexa `VERSION` plus `CHANGELOG.json` as one release transition. This keeps Update Center from observing a new version before its corresponding files/changelog are present.
