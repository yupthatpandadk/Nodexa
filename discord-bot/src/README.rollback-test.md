# Rollback test

In the test environment, verify the service can be stopped without affecting the Laravel panel, that moderation state remains on disk, and that restarting restores case numbering. This validates that Discord runtime failures do not take down Nodexa itself.
