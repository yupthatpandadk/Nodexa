# Non-goals for first release

The first runtime is not intended to replace Discord's own permission model, operate as a distributed multi-instance moderation cluster, or make irreversible moderation decisions without configured policy. Database-backed horizontal scaling and richer analytics can be layered on after the single-instance runtime is validated.
