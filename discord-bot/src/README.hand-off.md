# Deployment handoff

The server operator only needs to provide the Discord application values through `.env`, enable the two privileged intents, ensure the bot is invited with the required permissions, point the config path at the installed Nodexa panel and start the included service. No bot token is required in GitHub or in the admin module JSON.
