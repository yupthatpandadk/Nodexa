# Integration gate

Required external proof: bot connects, commands register, configured roles are honored, moderation actions work against permitted test users, logs arrive, AutoMod respects exemptions, raid protection can be recovered, and no privileged intent errors occur.
