# Current limitations

The repository implementation has not been run against the user's actual Discord application/guild from this environment. Therefore bot authorization, privileged intents, role hierarchy, channel overwrite restoration and real Discord API behavior are not yet production-verified. These are deployment/integration constraints rather than missing repository scaffolding.
