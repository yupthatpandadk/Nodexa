# Testing

`npm run check` validates runtime source syntax. `npm test` runs unit tests for policy parsing, persistent case/warn storage, raid thresholds, domains, appeals, validation, rate limiting, text normalization and runtime helpers. GitHub Actions executes both checks for changes under `discord-bot/**`.
