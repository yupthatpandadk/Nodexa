# Versioning

The runtime currently identifies itself as `0.1.0` on the feature branch. This is independent from Nodexa's stable VERSION. Nodexa VERSION/CHANGELOG should be bumped only when the runtime branch has passed the production checklist and is merged to main, keeping Update Center truthful and atomic.
