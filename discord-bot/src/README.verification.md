# Verification boundary

GitHub/CI can verify source, syntax and unit behavior. Only Discord can verify application authorization, privileged intents, guild membership, role hierarchy, channel overwrites and API-side moderation permissions. Both layers are required before calling the feature production-ready.
