# Final repository note

Do not merge this branch merely because files exist. The code now provides a real moderation runtime foundation, but Discord is an external system and destructive moderation requires validation with the actual application/guild. After that validation, merge atomically with Nodexa VERSION and CHANGELOG updates so Update Center never advertises an unverified release.
