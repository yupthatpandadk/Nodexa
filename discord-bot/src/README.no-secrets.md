# No-secrets policy

Repository files may contain example variable names and synthetic IDs only. Do not commit bot tokens, real `.env` files, webhook secrets or private moderation exports. `.env` is ignored and Docker build context excludes it.
