# Admin integration

The existing Nodexa Moderation Control Center remains the configuration surface. Saving moderation settings writes the module JSON consumed by the runtime. The runtime does not duplicate those controls or require staff to edit bot source code for thresholds, roles, channels or policy toggles.
