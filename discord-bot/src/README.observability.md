# Observability

The runtime includes structured logger, health snapshot, metrics counters and a status-file writer foundation. systemd logs can be inspected through journalctl. A future panel widget can read a deliberately sanitized status file/API rather than exposing process environment or Discord credentials.
