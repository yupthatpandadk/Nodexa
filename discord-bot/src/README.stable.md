# Stable criteria

Stable means: CI passes, runtime starts with the actual Nodexa config, commands register, permissions/role hierarchy behave correctly, cases persist, AutoMod detectors and exemptions behave correctly, anti-raid can be recovered safely, and appeals work. Only then should Nodexa VERSION/CHANGELOG be advanced.
