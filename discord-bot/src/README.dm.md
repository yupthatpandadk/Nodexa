# Punishment DMs

When enabled, the runtime attempts to DM the affected user with the Case-ID, action and reason. DM failure is non-fatal because users can disable DMs. Staff logs remain the authoritative operational record.
