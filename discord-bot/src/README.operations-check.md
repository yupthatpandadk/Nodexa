# Post-start checks

After starting the service, confirm it remains active, logs `Nodexa Moderation online`, slash commands appear in the test guild, config changes are observed, and the data directory receives case state only after a test moderation action. Stop immediately if role/channel behavior differs from the test plan.
