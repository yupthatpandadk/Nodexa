# Future admin UI

A later Moderation Cases page can consume a database-backed version of the runtime store to show open/history cases, active warns, appeal status and staff actions. That UI should not be treated as a prerequisite for the Discord runtime itself, but the current structured data model is designed to support it.
