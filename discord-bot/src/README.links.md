# Link policy

When link filtering is enabled, the runtime extracts HTTP(S) hostnames and compares them against configured allowed domains. Subdomains of an allowed domain can be accepted while lookalike domains are not. Discord invite filtering remains separately configurable.
