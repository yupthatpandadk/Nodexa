# Troubleshooting

If slash commands do not appear, verify client/guild IDs and bot authorization. If AutoMod does not see message text, enable Message Content Intent. If member joins are unavailable, enable Server Members Intent. For failed punishments, check the bot role position and required Discord permissions. Service logs are available through `journalctl -u nodexa-discord`.
