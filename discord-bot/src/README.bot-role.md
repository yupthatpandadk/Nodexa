# Bot role placement

Place the Nodexa bot role above normal members and roles it is expected to moderate, but below protected administrators/owners. Discord itself prevents many actions against higher roles; Nodexa also includes target hierarchy checks as a second safety layer for command handling.
