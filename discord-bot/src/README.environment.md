# Environment

Required: `DISCORD_TOKEN`, `DISCORD_CLIENT_ID`, `DISCORD_GUILD_ID`.

Optional: `NODEXA_MODULE_CONFIG`, `NODEXA_MODERATION_STORE`, `NODEXA_APPEALS_STORE`, `NODEXA_AUDIT_LOG`, `NODEXA_RUNTIME_STATUS`.

Secrets belong in `.env`/service secret management and are ignored by Git.
