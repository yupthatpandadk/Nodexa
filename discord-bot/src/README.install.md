# Install summary

From the Nodexa checkout, enter `discord-bot`, install dependencies, copy `.env.example` to `.env`, fill the three Discord values, verify `NODEXA_MODULE_CONFIG`, then install/start `nodexa-discord.service`. See `DEPLOY.md` and `PRODUCTION_CHECKLIST.md` for the full release gate.
