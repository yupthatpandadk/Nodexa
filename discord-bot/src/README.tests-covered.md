# Unit coverage areas

Current tests cover case/warn persistence, escalation parsing, raid thresholds, domains, appeals persistence/validation, configuration normalization, text normalization, rate limiting, data retention, event deduplication, safe log text, startup diagnostics, backups, runtime metadata and module loading. Live Discord behavior remains integration coverage.
