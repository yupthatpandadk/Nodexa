# Discord permissions

The bot needs only the permissions required by enabled features: View Channels, Send Messages, Embed Links, Manage Messages for AutoMod/purge, Moderate Members for timeouts, Kick Members, Ban Members, and Manage Channels when lock/unlock or Auto Lockdown is enabled. Keep the bot role below protected administrators and above roles it must moderate. The runtime includes target hierarchy helpers so production command handlers can reject unsafe targets.
