# Warn policy

Warnings persist with Case-IDs and can expire after the configured number of days. Threshold fields such as `warn_3_action`, `warn_5_action`, `warn_7_action` and `warn_10_action` support policies such as `timeout:30`, `kick`, `ban` or `review`. Test escalation with test accounts before production use.
