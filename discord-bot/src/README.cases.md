# Cases

Every warn, timeout, kick and ban can be represented by a persistent case with a stable Case-ID. Warnings are indexed per user so configured expiry and escalation rules can be evaluated. Logs and user DMs reference the same Case-ID, making support and staff review easier to correlate.
