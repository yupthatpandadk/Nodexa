# Commands

Staff: `/warn`, `/warnings`, `/timeout`, `/kick`, `/ban`, `/purge`, `/lock`, `/unlock`, and the appeal review command foundation. Public appeal submission is designed around `/appeal`. Staff access is additionally checked against the Moderator, Senior Moderator and Administrator role IDs configured in Nodexa, rather than relying only on Discord's command visibility.
