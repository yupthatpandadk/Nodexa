import fs from 'node:fs';import path from 'node:path';const file=process.env.NODEXA_APPEALS_STORE||path.resolve('data/appeals.json');function read(){try{return JSON.parse(fs.readFileSync(file,'utf8'))}catch{return []}}function write(v){fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,JSON.stringify(v,null,2))}export function createAppeal({caseId,userId,reason}){const a=read(),item={id:`APPEAL-${Date.now().toString(36).toUpperCase()}`,case_id:caseId,user_id:userId,reason,status:'pending',created_at:new Date().toISOString()};a.push(item);write(a);return item}export function setAppealStatus(id,status,staffId){const a=read(),x=a.find(v=>v.id===id);if(!x)return null;x.status=status;x.reviewed_by=staffId;x.reviewed_at=new Date().toISOString();write(a);return x}
