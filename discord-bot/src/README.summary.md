# Branch summary

`feature/moderation-runtime` now contains the standalone Nodexa Discord moderation service and supporting policy/state/deployment/test modules. The branch is intentionally isolated until CI and a real Discord test guild confirm external integration behavior. Stable `main`, VERSION and Update Center release metadata are therefore unchanged by this runtime work.
