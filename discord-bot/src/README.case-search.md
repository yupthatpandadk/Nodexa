# Case search model

Stable Case-IDs and stored user/moderator IDs are designed so a future Nodexa admin case browser can search by Case-ID, Discord user ID, moderator ID, action and date. The runtime store keeps these fields structured rather than embedding them only in Discord log text.
