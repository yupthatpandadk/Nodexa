# Moderation engine flow

Message event → load current Nodexa policy → exemption check → detectors → moderation action → configured log. Staff command → access check → target/action → persistent Case-ID → optional DM → punishment → case log. Member join → account-age signal → join-rate detector → optional lockdown → raid log.
