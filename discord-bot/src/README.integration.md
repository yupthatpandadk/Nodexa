# Integration boundary

The runtime integrates with Nodexa through the moderation configuration file and with Discord through the bot gateway/API. It does not require Laravel to maintain a long-running gateway connection, keeping the web panel and Discord process isolated operationally.
