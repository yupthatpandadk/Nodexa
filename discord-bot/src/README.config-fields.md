# Consumed config fields

Runtime support includes module enabled state, AutoMod toggles/thresholds, blocked words, allowed domains, anti-raid join/age/lockdown settings, warn expiry/escalation actions, staff role IDs, exempt IDs, punishment DMs and moderation/AutoMod/case log channel IDs. Unknown config keys are ignored so panel configuration remains forward-compatible.
