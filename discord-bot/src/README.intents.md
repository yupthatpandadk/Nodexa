# Privileged intents

The current moderation runtime uses Guild Members and Message Content events. Enable **Server Members Intent** and **Message Content Intent** for the Discord application before testing. Without them, anti-raid/member join handling or message-content AutoMod will be incomplete even if the process itself is online.
