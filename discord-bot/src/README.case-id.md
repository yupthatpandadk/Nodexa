# Case IDs

Case IDs are sequential and persisted as `CASE-000001`, `CASE-000002`, and so on. They are designed to be searchable references shared between staff logs, user DMs, warnings and appeals. The sequence is stored with moderation state rather than derived from process uptime.
