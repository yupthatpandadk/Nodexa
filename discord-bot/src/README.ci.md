# CI

The feature branch includes `.github/workflows/discord-runtime-check.yml`. It installs the runtime dependencies, performs syntax checks and runs Node's built-in unit test suite whenever Discord runtime files change. CI validates repository code but does not replace a live Discord test-guild integration run.
