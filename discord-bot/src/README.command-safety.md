# Command safety

Destructive commands are registered with Discord moderation permission requirements and the runtime additionally checks configured staff roles. Before stable release, wire/verify target hierarchy checks for every member-targeting action so owner/protected/higher-role targets cannot be moderated through Nodexa.
