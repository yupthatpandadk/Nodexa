# AutoMod engines

The runtime supports spam/flood windows, duplicate message detection, mass mentions, CAPS ratio, Discord invite blocking, configured word blacklist, basic scam/phishing patterns and link/domain allowlisting. Configured exempt users, channels and roles bypass AutoMod. Destructive thresholds should be tuned in a test guild before production.
