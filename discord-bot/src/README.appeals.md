# Appeals

Appeals are modeled separately from cases. A user submits a Case-ID and explanation; the runtime creates a pending appeal with its own ID. Staff can approve or reject it, recording reviewer and review time. Command definitions and persistence/validation are present on the feature branch; live Discord interaction wiring must be verified in the test guild before stable release.
